# goit-rdb-hw-03
# Домашнє завдання 3: SQL DQL команди

---

## 📋 Опис завдань

### Завдання 1: Базова вибірка даних

#### 1.1 Вибрати всі стовпчики з products
**SQL команда:**
```sql
SELECT * FROM products;
```
**Результат:** 77 рядків з усіма колонками таблиці products  
**Скриншот:** `p1_1.png`

#### 1.2 Вибрати name, phone з shippers
**SQL команда:**
```sql
SELECT name, phone FROM shippers;
```
**Результат:** 3 рядки з назвами компаній-перевізників та їх телефонами  
**Скриншот:** `p1_2.png`

---

### Завдання 2: Агрегатні функції

**Завдання:** Знайти середнє, максимальне та мінімальне значення ціни

**SQL команда:**
```sql
SELECT 
    AVG(price) AS average_price,
    MAX(price) AS max_price,
    MIN(price) AS min_price
FROM products;
```

**Результати:**
- **Середня ціна (average_price):** 28.866364
- **Максимальна ціна (max_price):** 263.50
- **Мінімальна ціна (min_price):** 2.50

**Скриншот:** `p2.png`

---

### Завдання 3: DISTINCT з сортуванням

**Завдання:** Вибрати унікальні комбінації category_id та price, відсортувати за спаданням ціни, показати топ 10

**SQL команда:**
```sql
SELECT DISTINCT 
    category_id, 
    price
FROM products
ORDER BY price DESC
LIMIT 10;
```

**Результати:** 10 унікальних комбінацій з найвищими цінами

**Скриншот:** `p3.png`

---

### Завдання 4: Фільтрація з підрахунком

**Завдання:** Знайти кількість продуктів з ціною від 20 до 100

**SQL команда:**
```sql
SELECT 
    COUNT(*) AS product_count
FROM products
WHERE price BETWEEN 20 AND 100;
```

**Результат:** **36 продуктів** знаходяться в ціновому діапазоні від 20 до 100

**Скриншот:** `p4.png`

---

### Завдання 5: Групування з агрегацією

**Завдання:** Знайти кількість продуктів та середню ціну для кожного постачальника

**SQL команда:**
```sql
SELECT 
    supplier_id,
    COUNT(*) AS product_count,
    AVG(price) AS average_price
FROM products
GROUP BY supplier_id;
```

**Результат:** 29 постачальників з кількістю продуктів та середньою ціною

**Скриншот:** `p5.png`

---

## 📁 Структура репозиторію

```
goit-rdb-hw-03/
├── homework_queries.sql    # Всі SQL запити з коментарями
├── README.md               # Опис завдань та результатів
├── p1_1.png                # Завдання 1.1
├── p1_2.png                # Завдання 1.2
├── p2.png                  # Завдання 2
├── p3.png                  # Завдання 3
├── p4.png                  # Завдання 4
└── p5.png                  # Завдання 5
```

---

## 🎓 Використані SQL концепції

- SELECT з wildcard (*)
- Вибірка конкретних колонок
- Агрегатні функції: AVG(), MAX(), MIN(), COUNT()
- DISTINCT для унікальних значень
- ORDER BY для сортування
- LIMIT для обмеження результатів
- WHERE для фільтрації
- BETWEEN для діапазону значень
- GROUP BY для групування даних
